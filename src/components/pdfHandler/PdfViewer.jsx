import React from "react";

const PdfViewer = () => {
  return (
    <div
      className="pdf-container"
      style={{
        width: "98%",
        height: "600px",
        overflowY: "auto",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >

        
    </div>
  );
};

export default PdfViewer;
